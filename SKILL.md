---
name: code-commenter
description: Add concise Chinese comments to Java, JavaScript, TypeScript, or Python code when explicitly invoked for code annotation.
disable-model-invocation: true
---

# Code Commenter

为现有代码补充能说明业务意图与设计原因的简洁中文注释。此次任务只修改或新增注释，不改动任何代码内容。

## 不可变更代码

- 只允许新增、修改或删除注释；绝不可改动任何非注释内容。
- 不得修改逻辑、命名、类型、控制流、导入、依赖、测试、字符串、空白格式或代码顺序。
- 即使发现 bug、可重构处或注释与代码不一致，也只在最终说明中指出，不在此次注释任务中修复。
- 完成后确认变更仅包含注释；若无法保证，撤销非注释变更后再交付。

## 总则

- 只写代码本身看不出的信息：业务目的、约束、原因、风险或状态含义。
- 使用简洁中文；API、JWT 等专业术语保留英文。
- 注释说明“做什么”和“为什么”，不复述“怎么做”。不要描述依赖、调用关系、实参、返回值来源或实现细节。
- 只陈述能由代码、注释、类型契约或用户提供的上下文支持的事实；原因不明确时，宁可不写，不臆测审计、幂等、补偿或一致性等目的。
- 保留准确的既有注释；删除或改写与代码重复、过时或误导的注释。

## 注释层级

### 类与接口

- 为目标类或接口添加职责或设计目的说明，使用 1–2 句话。
- 不说明使用的框架、导入项或依赖服务。
- Java、JavaScript、TypeScript 使用 `/** ... */`，且必须位于所有注解或装饰器之上：文档注释 → 注解/装饰器 → 类或接口声明。
- Python 的装饰器必须位于 `class` 声明之前；类 docstring（`"""..."""`）作为类体第一句，不放在装饰器外侧。

### 方法与函数

- 文档注释依次包含：一句功能概述、核心步骤摘要、参数、返回值与异常。
- `public`、`protected` 与 `private` 方法均按本节添加方法注释；getter、setter 与自动生成代码除外。
- 核心步骤摘要使用编号列表；每项仅写 2–6 个字的步骤名，不写调用、参数或实现细节。
- 步骤名称必须忠于已有业务语义，不把状态更新、数据写入等实现动作虚构为额外的业务阶段。
- Java、JavaScript、TypeScript 使用 `@param`、`@return`、`@throws`；Python 在 docstring 中使用 `Args`、`Returns`、`Raises`。
- 没有返回值或可能抛出的业务异常时，不凭空添加对应字段。

### 方法体内的关键步骤

- 仅在业务阶段、校验规则、状态流转、不可逆操作或异常处理的上方添加一行注释。核心步骤摘要中列出的每个关键阶段，均应有一处对应的行内注释；连续操作属于同一阶段时，可用一条注释概括。
- 注释表达业务语义，例如“统一认证失败，避免泄露账号状态”。
- 不逐行解释赋值、循环、条件、调用、传参或返回；概括连续操作时也不得枚举其底层动作。

## 不添加注释

- getter、setter 与框架自动生成代码。
- 显而易见的赋值、计数、遍历、条件判断或返回语句。
- 仅复述方法名、变量名、类型、依赖、调用顺序或参数值的内容。

## 自检

提交前逐条检查：

1. 删除后代码仍能直接看出的注释。
2. 每条保留的注释都能回答业务“做什么”或“为什么”。
3. 方法步骤摘要短小、编号完整，且未泄露实现细节。
4. 文档注释格式符合目标语言惯例。
5. 没有为未知的业务目的、数据用途或失败后果编造解释。
